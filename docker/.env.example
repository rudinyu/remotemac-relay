# Copy to `.env` at the repo root (next to docker-compose.yml) and set a strong value.
# Every mesh node must join with this same token. Generate one, e.g.:
#   openssl rand -base64 24
REMOTEMAC_MESH_TOKEN=change-me-to-a-strong-secret
